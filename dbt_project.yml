name: 'gsc_trade_disrupt_elt'
version: '1.0.0'
config-version: 2

profile: 'gsc_trade_disrupt_elt'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  gsc_trade_disrupt_elt:

    # Staging Layer
    staging:
      +materialized: view

    # Mart Layer
    marts:
      +materialized: table

seeds:
  gsc_trade_disrupt_elt:
    +quote_columns: false

tests:
  +store_failures: true